public static Integer findLargest(List<Integer> nums) {
	//code here
    Integer largestNumber = nums[0];
    for(Integer num : nums){
        if(num >= largestNumber){
            largestNumber = num;
        }
    }

    return largestNumber;
}