public void escalateIfMeetsCriteria(Case c) {
    if(c != null){
        if((c.Type == 'Mechanical' || c.Type == 'Breakdown') && c.Reason == 'Breakdown' && c.Status == 'In Progress'){
            c.IsEscalated = true;
        }
    }
    update c;
}